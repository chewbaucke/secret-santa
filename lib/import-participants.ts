import { Participant } from './secret-santa';

/**
 * Import participants from the original participants.json file
 * This is a one-time import utility
 */
export function importFromJSON(): Participant[] {
  // This would be used if we wanted to import from the JSON file
  // For now, users can manually add participants through the UI
  // or we could add an import feature later
  return [];
}

